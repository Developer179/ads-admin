# Backend base URL (univest-backend). No trailing slash. The browser calls it directly.
# e.g. https://uat-api.univest.in   (or http://localhost:8082 for local)
NEXT_PUBLIC_BACKEND_URL=https://dev-api.univest.in
